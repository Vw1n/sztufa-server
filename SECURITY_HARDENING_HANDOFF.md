# 会员安全修复交接（2026-08-27）

## 必须先迁移，再启动新代码

新增迁移 `20260827120000_track_card_upload_completion`，为 `CampusCardAsset` 增加 `uploadSettled`。
部署时先暂停注册/补交及旧清理进程，排空旧版本请求，再执行经核验环境中的 `prisma migrate deploy` 和 `prisma generate`，然后启动新版本和清理调度。
已在本地 Docker 专用验收库执行迁移；未迁移生产库或修改原有运行中服务。

## 本轮追加加固

- 生产启动要求 JWT_SECRET 和 MEMBER_JWT_SECRET 分别配置独立随机值，至少 32 字节；拒绝示例值、空白、低字符多样性值和相同密钥。长度/多样性检查不能证明随机性，应通过安全随机数生成器生成。生产缺少会员独立密钥将拒绝启动，切换密钥会使旧会话失效。
- 清理队列按 nextAttemptAt、deleteAfter、id 排序，让未决重试让出位置；未确认上传仍不释放容量。
- 登录增加真实滚动 10 分钟计数，保留原固定窗口外层限制。利用 AuthRateLimit 行锁跨进程串行记账，不需要新迁移。每个账号/IP只为允许进入该层的请求保留有界事件；达到限额的请求不延长滚动事件有效期。需要评估生产数据库额外事务负载。
- 管理员重置在事务内锁定并清除账号滚动事件，保留 IP 风控记录。持续分布式锁号仍需挑战验证或额外风控产品设计，当前未接入验证码或第三方服务。
- 两端 Vercel CSP 的 script-src 移除 unsafe-inline；保留 style-src 的内联样式兼容性。Token 存储方式未改为 Cookie，仍需要防范 XSS；非 Vercel 部署也必须配置相同响应头。
- BIZ-006 改为确认登录跳转、查找准确账号的已通过行、校验认证页准确标题；仍不是全程双端表单操作。

生产环境必须显式设置 `TRUST_PROXY`：使用实际受控代理 IP/CIDR 列表；直接连接填 `none`。默认开发环境不信任转发头。不要填 `true`、全部私网或 `/0`。

## 写入结果不明时保守保留

- 创建资产时 `uploadSettled=false`，只有 S3 PUT 成功返回并持久化后才变成 true。
- 在途或结果不明的上传即使 DELETE 后 HEAD 返回 404，也保持待清理状态、继续占用配额；清理任务会每分钟再次尝试。
- 进程崩溃、网络响应丢失、数据库无法确认写入结束时，不靠租约过期猜测 PUT 已停止。
- 代价：未决记录可能长期占用容量，需要运维告警和人工排查。不得仅因对象当前不存在就手工删除账本或改成 DELETED。应先确认旧写入者与存储请求均已终止，再按具体资产进行受审计的清理。
- 旧未绑定 STAGING/DELETE_PENDING 记录按未决处理；已经丢失账本或被旧版本误标 DELETED 的历史对象无法由本迁移自动识别，需另行受控核对。

## 验证边界

数据库集成测试使用唯一的 `DATABASE_URL`，仅允许本地专用测试库；存储 endpoint、桶、凭据必须显式提供。不要使用生产或日常共享库来验证。
容量测试用真实 PostgreSQL 事务，在两次 count 后同步争抢最后一份额度；P2034 最多重试三次，耗尽返回 503。
上传竞争测试调用真实 stage/cleanup，数据库集成层仅替换对象存储；HTTP 生命周期测试使用真实数据库、密码函数、守卫和配置的本地 MinIO。

## 日志与风险边界

Nest 日志和 E2E 文本日志/报告按敏感字段、Bearer/JWT、连接串、环境秘密与数字学号做脱敏。
BIZ-006/SEC-006 失败时不保存原始截图或 HTML；文本规则无法可靠清除图片中的隐私。
脱敏并非任意自由文本的完美识别器，业务代码仍禁止主动记录材料、密码及 Token；第三方直接写 stdout 的日志不保证经过 Nest 适配器。

分布式注册仍可能占满 500 份容量，持续攻击仍可能消耗账号登录次数；人工重置不是免疫锁号措施。BIZ-006 仍为 API 流程加浏览器结果校验，不是完整双端表单操作。
