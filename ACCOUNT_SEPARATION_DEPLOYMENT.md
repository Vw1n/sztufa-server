# 账号分离与校园卡审核部署说明

## 本次实现

- 新建 `MemberAccount`，原 `User` 仅保存工作人员，保留原后台外键，避免无意义地重命名全部业务接口。
- 普通用户使用 `/api/v1/member-auth/*`；工作人员使用 `/api/v1/staff-auth/*`。旧 `/auth` 仅兼容工作人员认证，`student-register` 已关闭。
- 双方 JWT 受众、账号类型和认证策略独立；旧无类型令牌失效。数据库会话版本支持重置密码、改权限、停用普通用户和普通用户退出后撤销凭证。
- 注册通过一次 multipart 提交账号资料与必填校园卡，不向匿名用户暴露可复用的任意上传链接。未提交成功的图片有独立持久化账本，能够清理孤立对象。
- 人工审核通过后在事务内生成删除任务，提交后立即执行第一次删除。预览立即被禁止，失败任务由可靠调度继续重试；后台区分“清理中”和“已删除”。
- 新密码使用 Node 内置 scrypt（N=32768/r=8/p=1）而不是新增原生 Argon2 依赖；兼容原 bcrypt 哈希，不截断长口令。15–128 字符限制适用于新建/重置密码；旧密码仍可登录。
- 仅保留普通用户的人工核验后密码重置入口。学校登录、邮箱服务、人机验证供应商、MFA、Cookie/CSRF 会话迁移没有在本次无配置状态下虚接，不能将这次改动视为全部安全建议均已上线。

## 上线前必需配置

校园卡不得使用现有公开图片桶。创建独立、无公共域名、无公共读权限的私有 R2 桶，使用仅限该桶的独立凭证：

```text
CARD_R2_ENDPOINT=
CARD_R2_BUCKET_NAME=
CARD_R2_ACCESS_KEY_ID=
CARD_R2_SECRET_ACCESS_KEY=
CARD_STORAGE_PRIVATE_CONFIRMED=true
MEMBER_JWT_SECRET=
CRON_SECRET=
CARD_CLEANUP_ENDPOINT=https://你的API域名/api/v1/internal/campus-card-cleanup
```

`CARD_STORAGE_PRIVATE_CONFIRMED` 是部署方完成存储权限检查后的确认，不是程序替存储加密或修改 ACL。没有这些配置时注册失败关闭，不回落到公开桶。必须验证源站匿名读取失败、公共图片代理不能访问该桶，且没有外留存原图的复制/版本/备份策略。本适配器针对专用 R2 桶；不能不经验证替换成开启版本留存的其他 S3 服务。

上传限制 3 MB、2000 万像素、10 秒图片处理超时；支持实际 JPG/PNG/WebP，服务端重新编码移除元数据。3 MB 为云函数请求体预留 multipart 余量。

## 可靠清理调度（上线门槛）

第一次删除在审核请求内等待执行；数据库与对象存储不是同一事务，失败必须有独立调度。选择部署环境支持的一种：

1. 用可靠调度器每分钟或每 5 分钟请求 `GET /api/v1/internal/campus-card-cleanup`，携带 `Authorization: Bearer <CRON_SECRET>`；确认所在套餐允许该频率。
2. 在长期运行的受控主机上，通过进程管理器运行 `npm run cards:cleanup:worker`。它每 30 秒调用一次该端点，异常继续重试。

禁止使用 Serverless 响应后未等待的 Promise 代替调度。每次最多清理 25 个对象，任务租约 5 分钟，失败退避最多一小时；监控 DELETE_PENDING 队列年龄、重试次数及调度心跳，告警日志需接入部署平台告警。没有可靠调度及告警验证前不能声称失败重试已在生产可用。删除成功以对象 HEAD 返回 404 为依据，不把成功入队当作完成。

材料不进入常规数据库备份中的图片集合；对象账本独立于账号生命周期，不被账号级联删除。备份恢复时保留账本并将存留对象转为待清理，待审核用户要求重新提交材料，已经删除的照片不恢复。

## 数据迁移

1. 暂停账号、竞猜、审核及有关管理写入，保留一致性备份。
2. 在脱敏测试库先运行只读预检：显式设置 `ACCOUNT_MIGRATION_AUDIT_URL`，执行 `npx ts-node scripts/audit-account-migration.ts`。脚本仅输出按角色数量和异常关联数量。
3. 演练 `20260827090000_separate_member_accounts`。迁移为一个数据库事务：普通用户迁移到 MemberAccount，保留 ID、密码哈希、创建时间和竞猜关联，再删除旧表里的普通用户。工作人员有历史竞猜、普通用户有后台业务引用时中止，不猜测、不丢弃记录。
4. 历史普通用户状态为 LEGACY，原学号绑定暂保留以防冒领，必须补交校园卡并经审核才能参与新竞猜；历史积分不清零。此行为上线前应通知用户。存在学号冲突时人工核实，不自动抢占原绑定。
5. 部署后端与两个前端，强制重新登录；验证前后台凭证不能串用。
6. 核对账号数量、竞猜/积分、球队/报名/审批关系，并创建一份新格式备份进行完整恢复演练。

新备份必须包含 MemberAccount；旧版缺少该表的备份明确拒绝直接恢复，需要在隔离旧版本环境先恢复、迁移，再生成新备份。不要在生产强制跳过校验。回滚必须暂停写入并处理切换后增量，不能直接用旧备份覆盖新注册用户。

## 验证范围与限制

补充验收：缺失、过期或非当前版本的材料不能预览或审核通过，但可以填写原因退回补充。账号操作进行中禁止切换管理对象。补充修改后后端全量 337 项通过（2 项跳过）、admin 全量 138 项通过，两端构建及改动文件静态检查通过。本轮浏览器验收因本地预览服务启动被运行环境拦截而未继续，不代表已完成真实存储验收。

2026-08-27 最终本地回归：后端 49 个测试套件、336 项通过、2 项原有跳过；admin 28 个套件、129 项通过；网页端 10 个套件、39 项通过。三端生产构建通过，admin 包体积门禁通过；改动文件静态检查及 git diff 空白检查通过。注册页面已在隔离本地浏览器检查字段、密码显隐与布局。

独立 test 仓库中的相关业务用例已调整为“上传合成图片、审核后竞猜”，仅完成语法检查，尚未运行需要真实测试数据库和私有桶的端到端链路。

本地使用合成材料测试，未上传真实校园卡、未连接线上数据库执行迁移、未部署或推送代码。单元和 HTTP 边界测试覆盖材料必填、类型/大小限制、受众隔离、角色拒绝、审核版本、删除顺序、失败重试、预览撤销及共享限流逻辑。

在临时 PGlite（PostgreSQL WASM 引擎）中执行了完整旧结构和迁移 SQL，检查 ID/哈希/积分保留、后台角色约束、学号唯一性、重复申请不抢占、异常历史关联安全中止。它不替代真实 PostgreSQL 并发负载、线上 R2 权限、物理删除和定时调度验收。

部署后必须用非真实个人信息的测试材料走通“注册 → 审核 → 对象消失”，再模拟存储故障验证关闭预览、任务保留、重试恢复；不以 mock 存储测试替代真实私有桶验收。
